@using Places.Models;

@if(Model.Count == 0)
{
  <p>There are no Places in your Trips list</p>
}
<ul>
  @foreach (Place place in Model)
  {
    <li><a href='/places/@place.Id'>@place.CityName</a></li>
  }
</ul>